# Custom Game Engine
Name WIP

### Build and Run
```
mkdir build
cd build
cmake --build ..
```

Requirements
- OpenGL `sudo apt install libgl1-mesa-dev`
- GLEW  `sudo apt install libglew-dev`
- glfw3 `sudo apt install libglfw3-dev`
use these installations for Ubuntu, otherwise provide your own :)


